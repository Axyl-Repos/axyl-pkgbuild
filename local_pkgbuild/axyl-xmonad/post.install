post_install() {
    echo -e "\n** Please copy .xmonad/ folder from /etc/skel/.xmonad/ to ~/.xmonad/ **\n"
    echo -e "\n** Execute this command: cp -r /etc/skel/.xmonad/ ~/.xmonad/ **\n"
}

post_upgrade() {
    post_install
}
